// Declare internals

var internals = {};


exports.compile = function (string, options, callback) {

    var renderer = function (context, opt, next) {

        return next(null, string + '|' + JSON.stringify(context));
    };

    return callback(null, renderer);
}
